pipeline {

    agent any

    tools {
        jdk 'OpenJDK 17.0.4'
        python '3.12.0'
    }

    environment {
        GITHUB_TOKEN = credentials("GITHUB_TOKEN_KB")
    }

    stages {
         
        stage("pre-build") {
            steps {
                echo 'Starting the build Stage '
                sh 'printenv'
            }
        }

          
        stage("pre-build") {
            steps {
                echo 'Starting the build Stage'
            }
        }

                 
        stage("pre-build") {
            steps {
                echo 'Starting the build Stage'
            }
        }

    }

}

