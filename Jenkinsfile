pipeline {

    agent any

    tools {
        jdk 'JDK 17'
        python '3.12.0'
    }

    environment {
        GITHUB_TOKEN = credentials("GITHUB_TOKEN_KB")
    }

    stages {
         
        stage("pre-build") {
            steps {
                echo 'Starting the build Stage '
                sh 'printenv'
            }
        }

          
        stage("pre-build") {
            steps {
                echo 'Starting the build Stage'
            }
        }

                 
        stage("pre-build") {
            steps {
                echo 'Starting the build Stage'
            }
        }

    }

}

